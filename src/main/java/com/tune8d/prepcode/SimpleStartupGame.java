package com.tune8d.prepcode;

/*
METHOD public static void main(String[] args)
  DECLARE an int variable to hold the number of user guesses, named numOfGuess
  DECLARE a SimpleStartUp to initialize the SimpleStartUp object. Call it simplestart.
  DECLARE an int array that hold the location data (three consecutive cells on a single row of seven virtual cells). Call it locationData.
  COMPUTE a random number between 0 and 4 that will be the starting location cell position.
  ASSIGN the random numbers to the int array (location data).
  CALL setLocationCells() that takes an int array and pass the int array (location data).

  WHILE the Startup is still alive:
    GET user input from the command line.
    CHECK the guess by CALL checkYourself() that takes the user input.
    INCLEMENT numOfGuess by one.
    BREAK if the number of hits reach 3.

  PRINT the number of guesses.


 */


public class SimpleStartupGame {
    public static void main(String[] args) {

    }
}
